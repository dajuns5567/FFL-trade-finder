const OFF=new Set(["QB","RB","WR","TE"]);
const IDP=new Set(["DL","DE","DT","LB","DB","CB","S","IDP"]);
const REDUCED=.5,OFF_FLOOR=260,IDP_FLOOR=180,EXP=.32,OFF_SCALE=1000,IDP_SCALE=300,DUAL_IDP_SHARE=.35,DUAL_CAP=1.20;
const norm=s=>String(s||"").normalize("NFKD").replace(/[\u0300-\u036f]/g,"").replace(/[’']/g,"").replace(/[^a-z0-9]+/gi," ").trim().toLowerCase();
const positionsOf=p=>new Set((Array.isArray(p?.positions)?p.positions:[p?.position]).filter(Boolean).map(x=>String(x).toUpperCase()));
const reindex=rows=>[...(rows||[])].filter(r=>Number.isFinite(Number(r?.rank))&&String(r?.player||"").trim()).sort((a,b)=>Number(a.rank)-Number(b.rank)).map((r,i)=>({...r,modelRank:i+1}));
function channels(results){const offense=[],idp=[];for(const r of results||[]){if(!r?.valid)continue;const weight=r.reducedWeight?REDUCED:1;if(r.id==="combined-dynasty"){offense.push({name:"The IDP Show Combined Offense",weight,rows:reindex((r.rankings||[]).filter(x=>String(x?.position||"").toUpperCase()!=="IDP"))});idp.push({name:"The IDP Show Combined IDP",weight,rows:reindex((r.rankings||[]).filter(x=>String(x?.position||"").toUpperCase()==="IDP"))});continue}if(r.id==="draftsharks"){offense.push({name:r.source,weight,rows:reindex((r.rankings||[]).filter(x=>OFF.has(String(x?.position||"").toUpperCase())))});continue}if(r.id?.includes("idp")){idp.push({name:r.source,weight,rows:reindex(r.rankings)});continue}offense.push({name:r.source,weight,rows:reindex(r.rankings)})}return{offense,idp}}
function hit(channel,name,positions){const same=channel.rows.filter(r=>norm(r.player)===norm(name));if(same.length===1)return same[0].modelRank;if(same.length>1&&positions?.size){const byPos=same.filter(r=>positions.has(String(r?.position||"").toUpperCase()));if(byPos.length===1)return byPos[0].modelRank}return null}
function weighted(list,p,floor){const hits=[];for(const c of list){const rank=hit(c,p.name,p.positions);if(Number.isFinite(rank))hits.push({rank,weight:c.weight,source:c.name})}if(!hits.length)return{rank:null,hits};if(hits.length===1)return{rank:Math.max(floor,hits[0].rank),hits};const w=hits.reduce((s,x)=>s+x.weight,0);return{rank:hits.reduce((s,x)=>s+x.rank*x.weight,0)/w,hits}}
const curve=(rank,scale)=>Math.max(1,Math.round(scale/Math.pow(Math.max(1,rank),EXP)));
export function buildConsensusComposite(results,players){const c=channels(results),counts=new Map();for(const p of players||[]){const k=norm(p?.name);if(k)counts.set(k,(counts.get(k)||0)+1)}const byId={},byName={},detailsById={};for(const p of players||[]){const id=String(p?.id||"").trim(),name=String(p?.name||"").trim(),key=norm(name),positions=positionsOf(p);if(!id||!key||counts.get(key)!==1)continue;const hasOff=[...positions].some(x=>OFF.has(x));const hasIdp=[...positions].some(x=>IDP.has(x))||(!hasOff&&positions.size>0);const o=hasOff?weighted(c.offense,{name,positions},OFF_FLOOR):{rank:null,hits:[]};const d=hasIdp?weighted(c.idp,{name,positions},IDP_FLOOR):{rank:null,hits:[]};const offenseValue=Number.isFinite(o.rank)?curve(o.rank,OFF_SCALE):null,idpValue=Number.isFinite(d.rank)?curve(d.rank,IDP_SCALE):null;let value=null,kind="none";if(Number.isFinite(offenseValue)&&Number.isFinite(idpValue)){value=Math.min(Math.round(offenseValue*DUAL_CAP),Math.round(offenseValue+idpValue*DUAL_IDP_SHARE));kind="dual"}else if(Number.isFinite(offenseValue)){value=offenseValue;kind="offense"}else if(Number.isFinite(idpValue)){value=idpValue;kind="idp"}if(!Number.isFinite(value))continue;const detail={id,name,kind,consensusCompositeValue:value,offenseRank:o.rank,idpRank:d.rank,offenseValue,idpValue,offenseSources:o.hits.map(x=>x.source),idpSources:d.hits.map(x=>x.source)};byId[id]=value;byName[name.toLowerCase()]=value;detailsById[id]=detail}return{byId,byName,detailsById,sourceCounts:{offense:c.offense.length,idp:c.idp.length},ambiguousNames:[...counts].filter(([,n])=>n>1).map(([k])=>k)}}
