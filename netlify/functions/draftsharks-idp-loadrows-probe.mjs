const pageUrl='https://www.draftsharks.com/rankings/idp/superflex';
export default async()=>{const page=await (await fetch(pageUrl)).text();const m=page.match(/<script[^>]+src=["']([^"']*RankingsApp[^"']*)["']/i);if(!m)return new Response(JSON.stringify({ok:false,error:'RankingsApp script not found'}));const scriptUrl=new URL(m[1],pageUrl).href;const js=await (await fetch(scriptUrl)).text();const i=js.indexOf('async loadRows(');return new Response(JSON.stringify({ok:i>=0,scriptUrl,loadRows:i>=0?js.slice(i,Math.min(js.length,i+4500)):null},null,2),{headers:{'content-type':'application/json','cache-control':'no-store'}})};