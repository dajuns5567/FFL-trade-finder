(()=>{
'use strict';
const clamp=(a,x,b)=>Math.max(a,Math.min(x,b));
const eng=()=>window.tradeEngine96||window.tradeEngine98||{};
const av=x=>Math.max(0,Number(eng()?.assetValue?.(x))||0);
const raw=xs=>(xs||[]).reduce((s,x)=>s+av(x),0);
const fmt=n=>Number(n||0).toLocaleString(undefined,{maximumFractionDigits:1});
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const id=x=>String(x?.id??'');
const rankOf=x=>x?.type==='player'?Math.max(1,Number(window.playerRankValue?.(x)?.rank)||9999):0;
function fairness(give,recv){return window.section1V124?.fairness?.(give,recv)||window.tradeEngine96?.fairness?.(give,recv)}
function teamNameOf(v){return window.teamName?.(v)||`Team ${v}`}
function nameOf(x){return x?.type==='pick'?(x.name||`${x.season} R${x.round}`):(window.playerName?.(x.id)||id(x))}
function playerTeam(x){const p=window.state?.players?.[String(x?.id)]||window.state?.players?.[x?.id]||{};return p.team||p.team_abbr||p.team_name||x?.nfl_team||x?.pro_team||'FA'}
function playerMeta(x){return `${window.groupPos?.(x)||''} • ${playerTeam(x)} • overall #${rankOf(x)}`}
function pickMeta(x){const p=window.tradeValueNormalizationV125?.pickContext?.(x)||{},slot=Number(p.projectedSlot),orig=p.originalTeam||teamNameOf(x.original_owner),owner=p.currentOwnerTeam||teamNameOf(x.owner);return{line:`${x.season} R${x.round}${Number.isFinite(slot)?` • projected ${Number(x.round)}.${String(Math.round(slot)).padStart(2,'0')}`:''}`,ownership:`Original: ${orig} • Current owner: ${owner}`}}
function row(x){if(x.type==='pick'){const p=pickMeta(x);return`<div class="trade95-asset"><div><b>${esc(nameOf(x))}</b><div class="trade95-sub">${esc(p.line)}</div><div class="trade95-sub">${esc(p.ownership)}</div></div><div class="trade95-value">${fmt(av(x))}</div></div>`}return`<div class="trade95-asset"><div><b>${esc(nameOf(x))}</b><div class="trade95-sub">${esc(playerMeta(x))}</div></div><div class="trade95-value">${fmt(av(x))}</div></div>`}
function side(title,xs,r,adj,eff){return`<div class="trade95-side"><div class="trade95-side-title">${esc(title)}</div>${xs.map(row).join('')}<div class="trade95-total"><span>RAW ASSET TOTAL</span><b>${fmt(r)}</b></div>${adj>0?`<div class="trade97-adjust"><span>VALUE ADJUSTMENT</span><b>+${fmt(adj)}</b></div><div class="trade97-effective"><span>TRADE-ADJUSTED TOTAL</span><b>${fmt(eff)}</b></div>`:''}</div>`}
const label=f=>f?.rejected?'Fleeced!':Number(f?.score)>=94?'Excellent Fit':Number(f?.score)>=82?'Fair':'Negotiable';
const selected=()=>[...document.querySelectorAll('.shopCheck:checked')].map(x=>x._asset).filter(Boolean);
function fit(me,other,give,recv){try{return clamp(0,50+(Number(window.teamContextTradeFit90?.(me,other,document.getElementById('findMode')?.value||'balanced',give,recv))||0)*5,100)}catch(_){return 50}}
function posOf(x){return x.type==='pick'?'PICK':(window.groupPos?.(x)||'')}
function positionOK(xs){const p=document.getElementById('findPos')?.value||'ANY';return p==='ANY'||xs.some(x=>x.type==='player'&&posOf(x)===p)}
function candidatePackages(owned,target,tier,giveBest){const players=owned.filter(x=>x.type==='player'),picks=owned.filter(x=>x.type==='pick'&&Number(x.round)<=3),out=[],seen=new Set(),add=xs=>{const k=xs.map(x=>`${x.type}:${id(x)}`).sort().join('|');if(k&&!seen.has(k)){seen.add(k);out.push(xs)}};const eligible=players.filter(p=>positionOK([p])).sort((a,b)=>Math.abs(av(a)-target)-Math.abs(av(b)-target)||rankOf(a)-rankOf(b)).slice(0,24);eligible.forEach(add);for(const p of eligible.slice(0,18))for(const k of picks.slice().sort((a,b)=>Math.abs(av(a)-(target-av(p)))-Math.abs(av(b)-(target-av(p)))).slice(0,5))add([p,k]);for(let i=0;i<eligible.length;i++)for(let j=i+1;j<Math.min(eligible.length,i+9);j++)add([eligible[i],eligible[j]]);if(tier==='up')return out.filter(xs=>{const ps=xs.filter(x=>x.type==='player');return ps.length&&Math.min(...ps.map(rankOf))<giveBest});return out}
function generateRows(){const me=Number(document.getElementById('findTeam')?.value),give=selected(),tier=document.getElementById('tradeTier94')?.value||'neutral';if(!me||!give.length||tier==='draft')return[];const target=raw(give),gp=give.filter(x=>x.type==='player'),giveBest=gp.length?Math.min(...gp.map(rankOf)):9999,all=[];for(const tm of window.state?.teams||[]){const other=Number(tm.id);if(other===me)continue;const owned=(window.state?.allAssets||[]).filter(x=>Number(x.owner)===other);for(const recv of candidatePackages(owned,target,tier,giveBest)){const f=fairness(give,recv);if(!f||f.rejected||Number(f.score)<58)continue;const rec=Number(f.score)*.92+fit(me,other,give,recv)*.08;all.push({other,give,recv,f,recommend:rec,gap:Math.abs(Number(f.edgeEffective)||0)})}}
 const uniq=new Map();for(const r of all.sort((a,b)=>b.recommend-a.recommend||b.f.score-a.f.score||a.gap-b.gap)){const k=`${r.other}|${r.recv.map(x=>`${x.type}:${id(x)}`).sort().join('|')}`;if(!uniq.has(k))uniq.set(k,r)}return[...uniq.values()].slice(0,100)}
function card(r,i){const f=r.f,v=label(f);return`<div class="result trade124-card ${f.score>=94?'excellent':f.score>=82?'fair':'negotiable'}"><div class="trade95-head"><div><b>#${i+1} ${esc(teamNameOf(r.other))}</b><div class="trade95-sub">Recommendation ${Math.round(r.recommend)}/100</div></div><div class="trade95-score">${f.score}<span>/100</span><div>${esc(v)}</div></div></div><div class="trade95-grid">${side('YOU RECEIVE',r.recv,f.bRaw,f.bAdj,f.bEffective)}${side('YOU SEND',r.give,f.aRaw,f.aAdj,f.aEffective)}</div><div class="trade95-summary"><div><b>${esc(v)}</b><span>Effective difference ${Number(f.edgeEffective)>=0?'+':''}${fmt(f.edgeEffective)}</span><span>Recommendation ${Math.round(r.recommend)}/100</span></div></div><button class="secondary small rationaleBtn" type="button">Trade rationale</button><div class="rationaleBody" hidden><ul><li>Ranked by the existing fairness and partner-fit recommendation logic.</li><li>Additional results do not relax fairness standards; pagination only exposes more already-qualified outcomes.</li></ul></div></div>`}
function paginate(host){host.querySelectorAll('#loadMoreTrades125').forEach(x=>x.remove());const cards=[...host.querySelectorAll(':scope > .trade124-card')];cards.forEach((c,i)=>c.hidden=i>=5);if(cards.length<=5)return;const b=document.createElement('button');b.id='loadMoreTrades125';b.className='secondary';b.style.cssText='margin:12px auto 4px;display:block';const refresh=()=>{const n=cards.filter(c=>c.hidden).length;b.textContent=`Load more trades (${n} more)`};refresh();b.onclick=()=>{cards.filter(c=>c.hidden).slice(0,5).forEach(c=>c.hidden=false);cards.some(c=>c.hidden)?refresh():b.remove()};host.appendChild(b)}
function renderFinder(){const host=document.getElementById('finderResults'),rows=generateRows();if(!host)return;host.innerHTML=rows.length?rows.map(card).join(''):'<div class="empty">No trade met the current fairness, position, intent, and package requirements.</div>';host.querySelectorAll('.rationaleBtn').forEach(b=>b.onclick=()=>{const body=b.nextElementSibling;if(body){body.hidden=!body.hidden;b.textContent=body.hidden?'Trade rationale':'Hide rationale'}});paginate(host)}
function syncFinderRemoval(e){const b=e.target.closest?.('button[data-pid],[data-pid] button,.removeAsset,.removeSearchAsset');if(!b||b.closest?.('#finderGlobalResults'))return;const scope=b.closest?.('#finder,.tab,#findShop')||document;const pid=b.dataset?.pid||b.closest?.('[data-pid]')?.dataset?.pid||b.dataset?.id||b.closest?.('[data-id]')?.dataset?.id;if(!pid)return;const txt=(b.textContent||b.getAttribute?.('aria-label')||'').toLowerCase();if(!/[×x]|remove|clear/.test(txt)&&!b.classList.contains('removeAsset')&&!b.classList.contains('removeSearchAsset'))return;setTimeout(()=>{for(const box of document.querySelectorAll('.shopCheck'))if(String(box._asset?.id)===String(pid)){box.checked=false;box.dispatchEvent(new Event('change',{bubbles:true}))}},0)}
function cleanup(){for(const host of [document.getElementById('findShop'),document.getElementById('evalChooserA'),document.getElementById('evalChooserB')].filter(Boolean)){const walker=document.createTreeWalker(host,NodeFilter.SHOW_TEXT);let n;while((n=walker.nextNode()))n.nodeValue=(n.nodeValue||'').replace(/\s*\[rank\s+\d+\]/ig,'')}}
function bind(){if(document.__v125Runtime)return;document.__v125Runtime=true;document.addEventListener('click',e=>{syncFinderRemoval(e);const run=e.target.closest?.('#runFinder');if(run&&document.getElementById('tradeTier94')?.value!=='draft'){e.preventDefault();e.stopImmediatePropagation();setTimeout(renderFinder,0);return}const rb=e.target.closest?.('.rationaleBtn');if(rb){e.preventDefault();e.stopImmediatePropagation();const body=rb.nextElementSibling;if(body){body.hidden=!body.hidden;rb.textContent=body.hidden?'Trade rationale':'Hide rationale'}}},true)}
function install(){if(window.__v124RuntimePoll){clearInterval(window.__v124RuntimePoll);window.__v124RuntimePoll=null}window.tradeValueNormalizationV125?.install?.();bind();cleanup()}
setTimeout(install,0);setTimeout(install,250);setTimeout(install,900);if(!window.__v125RuntimePoll)window.__v125RuntimePoll=setInterval(install,1500);
window.section1V125={install,generateRows,renderFinder};
})();