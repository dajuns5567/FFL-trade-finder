(()=>{
'use strict';
const clamp=(a,x,b)=>Math.max(a,Math.min(x,b));
const st=()=>window.state||{};
const norm=()=>window.tradeValueNormalizationV139||window.tradeValueNormalizationV130||{};
const av=x=>Math.max(0,Number(norm().canonicalValue?.(x))||0);
const raw=xs=>(xs||[]).reduce((s,x)=>s+av(x),0);
const id=x=>String(x?.id??'');
const q=s=>String(s||'').normalize('NFKD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));
const fmt=n=>Number(n||0).toLocaleString(undefined,{maximumFractionDigits:0});
const pname=x=>x?.type==='pick'?(x.name||`${x.season} R${x.round}`):(window.playerName?.(x.id)||x?.name||id(x));
const pos=x=>x?.type==='pick'?'PICK':(window.groupPos?.(x)||'IDP');
const rank=x=>x?.type==='player'?Math.max(1,Number(window.playerRankValue?.(x)?.rank)||9999):0;
const teamName=n=>window.teamName?.(n)||((st().teams||[]).find(t=>Number(t.id)===Number(n))?.name)||`Team ${n}`;
const fair=(a,b)=>window.section1V130?.fair?.(a,b)||null;
let rows=[],visible=5,runToken=0;
function targetText(){return q(document.getElementById('desiredPlayerSearch')?.value)}
function mode(){return String(document.getElementById('findMode')?.value||'balanced')}
function tier(){return String(document.getElementById('tradeTier94')?.value||'neutral')}
function selectedGive(){return[...document.querySelectorAll('#findShop .shopCheck:checked,.shopCheck:checked')].map(x=>x._asset).filter(Boolean)}
function addAssets(){return!!document.getElementById('tradeAssist97')?.checked}
function active(){const m=mode();return!!targetText()&&addAssets()&&selectedGive().length>0&&tier()==='neutral'&&(m==='balanced'||m==='value')}
function owned(owner){return(st().allAssets||[]).filter(x=>Number(x.owner)===Number(owner)&&(x.type==='player'||(x.type==='pick'&&Number(x.round)<=3)))}
function key(xs){return(xs||[]).map(x=>`${x.type}:${id(x)}`).sort().join('|')}
function addPkg(out,seen,xs){const clean=(xs||[]).filter(Boolean),k=key(clean);if(clean.length&&!seen.has(k)){seen.add(k);out.push(clean)}}
function outgoing(me){const chosen=selectedGive(),out=[],seen=new Set();addPkg(out,seen,chosen);if(chosen.length>=3)return out;const used=new Set(chosen.map(x=>`${x.type}:${id(x)}`)),extras=owned(me).filter(x=>!used.has(`${x.type}:${id(x)}`)).sort((a,b)=>av(b)-av(a)).slice(0,20);for(const x of extras)addPkg(out,seen,[...chosen,x]);if(chosen.length===1)for(let i=0;i<extras.length&&out.length<90;i++)for(let j=i+1;j<extras.length&&out.length<90;j++)addPkg(out,seen,[chosen[0],extras[i],extras[j]]);return out}
function incoming(owner,target){const extras=owned(owner).filter(x=>!(x.type==='player'&&id(x)===id(target))).sort((a,b)=>av(b)-av(a)).slice(0,18),out=[[target]];for(const x of extras)out.push([target,x]);for(let i=0;i<extras.length;i++)for(let j=i+1;j<extras.length;j++)out.push([target,extras[i],extras[j]]);return out}
function positionsOK(target){const checked=[...document.querySelectorAll('#tradePos97 .trade97-pos:checked,.trade97-pos:checked')].map(x=>String(x.value||'').toUpperCase());return!checked.length||checked.includes('ANY')||checked.includes(pos(target))}
function fit(me,other,give,recv){try{return clamp(0,50+(Number(window.teamContextTradeFit90?.(me,other,mode(),give,recv))||0)*5,100)}catch{return 50}}
function maxValueOK(f){const s=Number(f?.score)||0,e=Number(f?.edgeEffective)||0;return s>=75&&s<=90&&e>0}
function score(f,pf,give,recv){if(mode()==='value'){const send=Math.max(1,Number(f?.aEffective)||0),receive=Number(f?.bEffective)||0,edge=Math.max(0,receive-send),adv=clamp(0,edge/send,.35)/.35*100,fairTarget=clamp(0,100-Math.abs((Number(f?.score)||0)-83)*6,100),pref=clamp(0,50+4*((give?.length||0)-(recv?.length||0))+2*Math.max(0,(give?.length||0)-1),100);return adv*.50+fairTarget*.30+pref*.15+pf*.05}return(Number(f?.score)||0)*.92+pf*.08}
function protectTargetOnly(list){const only=list.filter(r=>r.recv.length===1),other=list.filter(r=>r.recv.length!==1);if(!only.length)return list;const out=[];let oi=0,xi=0;const targetFirst=Math.min(3,only.length);while(oi<targetFirst)out.push(only[oi++]);while(out.length<list.length){if(xi<other.length)out.push(other[xi++]);else if(oi<only.length)out.push(only[oi++]);else break}return out}
function assetKey(x){return`${x?.type}:${id(x)}`}
function containsAllSelected(give,chosen){const have=new Set((give||[]).map(assetKey));return(chosen||[]).every(x=>have.has(assetKey(x)))}
function shapeAwareManual(list,chosen,limit=140){const ranked=protectTargetOnly(list).filter(r=>containsAllSelected(r.give,chosen)&&r.give.length>=chosen.length&&r.give.length<=3),out=[],used=new Set();if(!ranked.length)return ranked;const take=r=>{if(!r||used.has(r)||out.length>=limit)return false;used.add(r);out.push(r);return true},manualKey=key(chosen),window=ranked.slice(0,30),allowed=chosen.length===1?[1,2,3]:chosen.length===2?[2,3]:[3];take(ranked[0]);take(ranked.find(r=>key(r.give)===manualKey));const missingRecv=new Set([1,2,3]),missingGive=new Set(allowed);for(const r of out){missingRecv.delete(r.recv.length);missingGive.delete(r.give.length)}while((missingRecv.size||missingGive.size)&&out.length<7){let best=null,bestCover=0;for(const r of window){if(used.has(r))continue;const cover=(missingRecv.has(r.recv.length)?1:0)+(missingGive.has(r.give.length)?1:0);if(cover>bestCover){best=r;bestCover=cover;if(cover===2)break}}if(!best||!bestCover)break;take(best);missingRecv.delete(best.recv.length);missingGive.delete(best.give.length)}const targetOnlyCount=out.reduce((n,r)=>n+(r.recv.length===1?1:0),0);if(targetOnlyCount<2)take(window.find(r=>r.recv.length===1&&!used.has(r)));for(const r of ranked){if(out.length>=limit)break;take(r)}return out.slice(0,limit)}
function assetRow(x){if(x.type==='pick'){const label=typeof window.pickDisplay90==='function'?window.pickDisplay90(x):`<span><b>${esc(pname(x))}</b></span>`;return`<div class="trade95-asset"><div>${label}</div><div class="trade95-value">${fmt(av(x))}</div></div>`}const p=st().players?.[x.id]||{};return`<div class="trade95-asset"><div><b>${esc(pname(x))}</b><div class="trade95-sub">${esc(pos(x))} • ${esc(p.team||'FA')} • overall #${rank(x)}</div></div><div class="trade95-value">${fmt(av(x))}</div></div>`}
function side(title,xs,f,send){const total=send?f.aRaw:f.bRaw,adj=send?f.aAdj:f.bAdj,eff=send?f.aEffective:f.bEffective;return`<div class="trade95-side"><div class="trade95-side-title">${title}</div>${xs.map(assetRow).join('')}<div class="trade95-total"><span>RAW ASSET TOTAL</span><b>${fmt(total)}</b></div>${adj>0?`<div class="trade97-adjust"><span>VALUE ADJUSTMENT</span><b>+${fmt(adj)}</b></div><div class="trade97-effective"><span>TRADE-ADJUSTED TOTAL</span><b>${fmt(eff)}</b></div>`:''}</div>`}
function card(r,i){const f=r.f,label=f.score>=94?'Excellent Fit':f.score>=82?'Fair':'Negotiable',only=r.recv.length===1;return`<div class="result trade95-card"><div class="trade95-head"><div><b>#${i+1} ${esc(teamName(r.other))}</b><div class="trade95-sub">Specific target: ${esc(pname(r.target))}${only?' • target only':''}</div></div><div class="trade95-score">${Math.round(f.score)}<span>/100</span><div>${label}</div></div></div><div class="trade95-grid">${side('YOU RECEIVE',r.recv,f,false)}${side('YOU SEND',r.give,f,true)}</div><div class="trade95-summary"><div><b>${label}</b>${mode()==='value'?`<span>Effective value edge ${Number(f.edgeEffective)>=0?'+':''}${fmt(f.edgeEffective)}</span>`:''}<span>Partner fit ${Math.round(r.fit)}/100</span></div></div></div>`}
function draw(host){host.innerHTML=rows.length?rows.slice(0,visible).map(card).join(''):'<div class="empty">No realistic specific-player Add Assets trade passed the current requirements.</div>';if(visible<rows.length){const b=document.createElement('button');b.className='secondary';b.style.cssText='margin:12px auto 4px;display:block';b.textContent=`Load more trades (${rows.length-visible} more)`;b.onclick=()=>{visible=Math.min(rows.length,visible+5);draw(host)};host.appendChild(b)}}
async function run(){const my=++runToken,host=document.getElementById('finderResults'),me=Number(document.getElementById('findTeam')?.value),needle=targetText();if(!host||!needle)return;if(!me){host.innerHTML='<div class="empty">Choose your team before finding trades.</div>';return}const target=(st().allAssets||[]).find(x=>x?.type==='player'&&Number(x.owner)!==me&&q(pname(x))===needle);if(!target){host.innerHTML='<div class="empty">That player could not be matched exactly to a rostered player.</div>';return}if(!positionsOK(target)){host.innerHTML='<div class="empty">The selected specific player does not match the selected acquire-position filter.</div>';return}host.innerHTML=`<div class="empty">Searching realistic trades for ${esc(pname(target))}…</div>`;await new Promise(r=>setTimeout(r,0));const chosen=selectedGive(),gives=outgoing(me),recvs=incoming(Number(target.owner),target),result=[],seen=new Set();for(const recv of recvs){for(const give of gives){if(my!==runToken)return;const sig=key(give)+'>'+key(recv);if(seen.has(sig))continue;seen.add(sig);const f=fair(give,recv);if(!f||f.rejected||(mode()==='value'&&!maxValueOK(f)))continue;const pf=fit(me,Number(target.owner),give,recv),r={other:Number(target.owner),target,give,recv,f,fit:pf};r.score=score(f,pf,give,recv);result.push(r)}if(result.length%250===0)await new Promise(r=>setTimeout(r,0))}result.sort((a,b)=>b.score-a.score||(Number(b.f?.score)||0)-(Number(a.f?.score)||0)||a.recv.length-b.recv.length);rows=shapeAwareManual(result,chosen,140);visible=Math.min(5,rows.length);draw(host)}
document.addEventListener('click',e=>{const b=e.target.closest?.('#runFinder');if(!b||!active())return;e.preventDefault();e.stopImmediatePropagation();run().catch(err=>{console.error('V308 specific-player Add Assets failed',err);const h=document.getElementById('finderResults');if(h)h.innerHTML='<div class="empty">Specific-player Add Assets search stopped unexpectedly.</div>'})},true);
window.tradeSpecificAddAssetsV282={run,active};
})();
